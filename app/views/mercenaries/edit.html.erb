<div class="container">
  <h1 class="text-center">Modifier le profil de <%= @mercenary.name %></h1>
  <%= render "form", mercenary: @mercenary %>

  <div class="back-button">
    <%= link_to "Retour au profil du mercenaire", mercenary_path(@mercenary), class: "btn btn-secondary" %>
  </div>
</div>
